import { CurrentyFormatPipe } from './currenty-format.pipe';

describe('CurrentyFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new CurrentyFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
